{%- comment -%}
  Kofii Flow — Timeline
  ---------------------------------------------------------------------------
  A sequence of moments on a spine: founding dates, a process, a roadmap.

  Two layouts:
    vertical      spine on the left, entries stacked beside it
    alternating   spine down the centre, entries zig-zagging either side on
                  desktop, collapsing to the vertical layout below 990px

  The spine draws itself in as each entry arrives. That uses no new JavaScript:
  Kofii Motion already puts `data-kf-inview` on an entry when it enters, so the
  spine segment is a child styled from that state. It also means the drawing
  inherits the whole safety model — no JS, reduced motion or Flow Mode off and
  the spine is simply drawn already.

  Blocks are section-local: a timeline entry has a fixed shape (date, heading,
  text) and is not a content primitive worth exposing to other sections.
{%- endcomment -%}

{{ 'section-timeline.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign s = section.settings

  assign timeline_class = 'kf-timeline kf-timeline--' | append: s.layout
  assign timeline_class = timeline_class | append: ' kf-timeline--marker-' | append: s.marker

  assign media_sizes = '(min-width: 990px) 40vw, 100vw'
-%}

<div {% render 'kf-section-attrs', s: section.settings, extra_class: timeline_class %}>
  <div class="kf-container">
    {%- if s.heading != blank or s.eyebrow != blank -%}
      <header class="kf-section-header kf-timeline__header{% if s.layout == 'alternating' %} kf-section-header--center{% endif %}">
        {%- if s.eyebrow != blank -%}
          <p class="kf-label kf-section-header__eyebrow">{{ s.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if s.heading != blank -%}
          <h2 class="{{ s.heading_size }}">{{ s.heading }}</h2>
        {%- endif -%}
      </header>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      {%- comment -%}
        Stagger contract: the LIST carries data-kf-stagger, each ITEM carries
        data-kf-animate. Putting the animation on the list would animate the
        whole timeline as one block and the entries would never sequence.
      {%- endcomment -%}
      <ol
        class="kf-timeline__list"
        role="list"
        {% render 'kf-motion-attrs',
          stagger: true,
          stagger_delay: s.animation_stagger_delay,
          trigger: s.animation_trigger %}
      >
        {%- for block in section.blocks -%}
          <li
            class="kf-timeline__item"
            {{ block.shopify_attributes }}
            {% render 'kf-motion-attrs',
              animation: s.animation,
              duration: s.animation_duration,
              easing: s.animation_easing %}
          >
            <div class="kf-timeline__marker" aria-hidden="true">
              <span class="kf-timeline__dot">
                {%- if s.marker == 'number' -%}
                  {{- forloop.index -}}
                {%- endif -%}
              </span>
              {%- unless forloop.last -%}
                <span class="kf-timeline__spine">
                  <span class="kf-timeline__spine-fill"></span>
                </span>
              {%- endunless -%}
            </div>

            <div class="kf-timeline__body">
              {%- if block.settings.date != blank -%}
                <p class="kf-label kf-timeline__date">{{ block.settings.date | escape }}</p>
              {%- endif -%}

              {%- if block.settings.heading != blank -%}
                <h3 class="kf-timeline__heading {{ s.item_heading_size }}">{{ block.settings.heading }}</h3>
              {%- endif -%}

              {%- if block.settings.image != blank -%}
                <div class="kf-timeline__media">
                  {%- render 'kf-media',
                    image: block.settings.image,
                    ratio: s.media_ratio,
                    sizes: media_sizes
                  -%}
                </div>
              {%- endif -%}

              {%- if block.settings.text != blank -%}
                <div class="kf-rte kf-timeline__text">{{ block.settings.text }}</div>
              {%- endif -%}

              {%- if block.settings.link_label != blank -%}
                {%- render 'kf-button',
                  label: block.settings.link_label,
                  href: block.settings.link,
                  style: 'plain',
                  icon: 'arrow'
                -%}
              {%- endif -%}
            </div>
          </li>
        {%- endfor -%}
      </ol>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.kf-timeline.name",
  "tag": "section",
  "class": "kf-timeline-section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.kf-timeline.settings.header__1.content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:sections.kf-timeline.settings.eyebrow.label"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:sections.kf-timeline.settings.heading.label",
      "default": "Our story so far"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:sections.kf-timeline.settings.heading_size.label",
      "options": [
        {
          "value": "kf-h3",
          "label": "t:sections.kf-timeline.settings.heading_size.options__1.label"
        },
        {
          "value": "kf-h2",
          "label": "t:sections.kf-timeline.settings.heading_size.options__2.label"
        },
        {
          "value": "kf-h1",
          "label": "t:sections.kf-timeline.settings.heading_size.options__3.label"
        }
      ],
      "default": "kf-h2"
    },
    {
      "type": "select",
      "id": "item_heading_size",
      "label": "t:sections.kf-timeline.settings.item_heading_size.label",
      "options": [
        {
          "value": "kf-h5",
          "label": "t:sections.kf-timeline.settings.item_heading_size.options__1.label"
        },
        {
          "value": "kf-h4",
          "label": "t:sections.kf-timeline.settings.item_heading_size.options__2.label"
        },
        {
          "value": "kf-h3",
          "label": "t:sections.kf-timeline.settings.item_heading_size.options__3.label"
        }
      ],
      "default": "kf-h4"
    },
    {
      "type": "header",
      "content": "t:sections.kf-timeline.settings.header__2.content"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:sections.kf-timeline.settings.layout.label",
      "options": [
        {
          "value": "vertical",
          "label": "t:sections.kf-timeline.settings.layout.options__1.label"
        },
        {
          "value": "alternating",
          "label": "t:sections.kf-timeline.settings.layout.options__2.label"
        }
      ],
      "default": "vertical",
      "info": "t:sections.kf-timeline.settings.layout.info"
    },
    {
      "type": "select",
      "id": "marker",
      "label": "t:sections.kf-timeline.settings.marker.label",
      "options": [
        {
          "value": "dot",
          "label": "t:sections.kf-timeline.settings.marker.options__1.label"
        },
        {
          "value": "number",
          "label": "t:sections.kf-timeline.settings.marker.options__2.label"
        }
      ],
      "default": "dot"
    },
    {
      "type": "select",
      "id": "media_ratio",
      "label": "t:sections.kf-timeline.settings.media_ratio.label",
      "info": "t:sections.kf-timeline.settings.media_ratio.info",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.kf-timeline.settings.media_ratio.options__1.label"
        },
        {
          "value": "square",
          "label": "t:sections.kf-timeline.settings.media_ratio.options__2.label"
        },
        {
          "value": "landscape",
          "label": "t:sections.kf-timeline.settings.media_ratio.options__3.label"
        },
        {
          "value": "wide",
          "label": "t:sections.kf-timeline.settings.media_ratio.options__4.label"
        }
      ],
      "default": "landscape"
    },
    {
      "type": "header",
      "content": "t:sections.kf-timeline.settings.header__3.content"
    },
    {
      "type": "select",
      "id": "animation",
      "label": "t:sections.kf-timeline.settings.animation.label",
      "info": "t:sections.kf-timeline.settings.animation.info",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-timeline.settings.animation.options__1.label"
        },
        {
          "value": "fade",
          "label": "t:sections.kf-timeline.settings.animation.options__2.label"
        },
        {
          "value": "fade-up",
          "label": "t:sections.kf-timeline.settings.animation.options__3.label"
        },
        {
          "value": "fade-right",
          "label": "t:sections.kf-timeline.settings.animation.options__4.label"
        },
        {
          "value": "blur-in",
          "label": "t:sections.kf-timeline.settings.animation.options__5.label"
        }
      ],
      "default": "fade-up"
    },
    {
      "type": "range",
      "id": "animation_stagger_delay",
      "label": "t:sections.kf-timeline.settings.animation_stagger_delay.label",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "ms",
      "default": 80
    },
    {
      "type": "select",
      "id": "animation_duration",
      "label": "t:sections.kf-timeline.settings.animation_duration.label",
      "options": [
        {
          "value": "theme",
          "label": "t:sections.kf-timeline.settings.animation_duration.options__1.label"
        },
        {
          "value": "fast",
          "label": "t:sections.kf-timeline.settings.animation_duration.options__2.label"
        },
        {
          "value": "normal",
          "label": "t:sections.kf-timeline.settings.animation_duration.options__3.label"
        },
        {
          "value": "slow",
          "label": "t:sections.kf-timeline.settings.animation_duration.options__4.label"
        }
      ],
      "default": "theme"
    },
    {
      "type": "select",
      "id": "animation_easing",
      "label": "t:sections.kf-timeline.settings.animation_easing.label",
      "options": [
        {
          "value": "theme",
          "label": "t:sections.kf-timeline.settings.animation_easing.options__1.label"
        },
        {
          "value": "standard",
          "label": "t:sections.kf-timeline.settings.animation_easing.options__2.label"
        },
        {
          "value": "smooth",
          "label": "t:sections.kf-timeline.settings.animation_easing.options__3.label"
        },
        {
          "value": "out",
          "label": "t:sections.kf-timeline.settings.animation_easing.options__4.label"
        },
        {
          "value": "spring",
          "label": "t:sections.kf-timeline.settings.animation_easing.options__5.label"
        }
      ],
      "default": "theme"
    },
    {
      "type": "select",
      "id": "animation_trigger",
      "label": "t:sections.kf-timeline.settings.animation_trigger.label",
      "options": [
        {
          "value": "scroll",
          "label": "t:sections.kf-timeline.settings.animation_trigger.options__1.label"
        },
        {
          "value": "load",
          "label": "t:sections.kf-timeline.settings.animation_trigger.options__2.label"
        }
      ],
      "default": "scroll"
    },
    {
      "type": "header",
      "content": "t:sections.kf-timeline.settings.header__4.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.kf-timeline.settings.color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "section_transition",
      "label": "t:sections.kf-timeline.settings.section_transition.label",
      "info": "t:sections.kf-timeline.settings.section_transition.info",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-timeline.settings.section_transition.options__1.label"
        },
        {
          "value": "round",
          "label": "t:sections.kf-timeline.settings.section_transition.options__2.label"
        },
        {
          "value": "overlap",
          "label": "t:sections.kf-timeline.settings.section_transition.options__3.label"
        }
      ],
      "default": "none"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:sections.kf-timeline.settings.padding_top.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:sections.kf-timeline.settings.padding_bottom.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "t:sections.kf-timeline.blocks.item.name",
      "limit": 12,
      "settings": [
        {
          "type": "text",
          "id": "date",
          "label": "t:sections.kf-timeline.blocks.item.settings.date.label",
          "default": "2019"
        },
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.kf-timeline.blocks.item.settings.heading.label",
          "default": "Where it began"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.kf-timeline.blocks.item.settings.text.label",
          "default": "<p>A sentence or two on what happened, and why it mattered.</p>"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.kf-timeline.blocks.item.settings.image.label"
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "t:sections.kf-timeline.blocks.item.settings.link_label.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.kf-timeline.blocks.item.settings.link.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.kf-timeline.presets.name",
      "blocks": [
        {
          "type": "item",
          "settings": {
            "date": "2019",
            "heading": "Where it began"
          }
        },
        {
          "type": "item",
          "settings": {
            "date": "2022",
            "heading": "The first workshop"
          }
        },
        {
          "type": "item",
          "settings": {
            "date": "Today",
            "heading": "What comes next"
          }
        }
      ]
    }
  ]
}
{% endschema %}
