{%- comment -%}
  Kofii Flow — Testimonials
  ---------------------------------------------------------------------------
  Customer quotes in a grid, with an optional star rating, avatar and
  attribution on each.

  A grid and not a carousel, on purpose. Social proof works by volume: three
  quotes visible at once say more than one quote behind a control most
  visitors never touch. On a phone, where three cards will not fit, the row
  scrolls with CSS scroll-snap rather than a slider element — so nothing is
  duplicated from the sections that do carry one.

  Ratings render through `snippets/kf-stars.liquid`, the same one the product
  card and the rating block use.

  Blocks are section-local: a testimonial is a quote plus an attribution, which
  has no meaning outside this section.
{%- endcomment -%}

{{ 'section-testimonials.css' | asset_url | stylesheet_tag }}
{%- if section.settings.layout == 'carousel' -%}
  {{ 'component-slider.css' | asset_url | stylesheet_tag }}
  {%- comment -%}
    Only when something needs driving. A carousel with no arrows and no
    pagination is a plain scroll-snap row and stays JavaScript-free.
  {%- endcomment -%}
  {%- if section.settings.show_arrows or section.settings.pagination != 'none' -%}
    <script src="{{ 'kf-slider.js' | asset_url }}" defer></script>
  {%- endif -%}
{%- endif -%}

{%- liquid
  assign s = section.settings
  assign is_carousel = false
  if s.layout == 'carousel'
    assign is_carousel = true
  endif

  assign needs_slider = false
  if is_carousel and s.show_arrows
    assign needs_slider = true
  endif
  if is_carousel and s.pagination != 'none'
    assign needs_slider = true
  endif

  assign has_header = false
  if s.heading != blank or s.eyebrow != blank or s.text != blank
    assign has_header = true
  endif

  assign list_class = 'kf-testimonials'
  if s.mobile_layout == 'scroll'
    assign list_class = list_class | append: ' kf-testimonials--scroll-mobile'
  endif

  comment
    The carousel sizes its slides from --kf-slide-count, the grid from its own
    column count. Same number, two different properties.
  endcomment
  assign list_style = '--kf-slide-count: ' | append: s.columns_desktop | append: ';'
  assign list_style = list_style | append: ' --kf-testimonials-cols: ' | append: s.columns_desktop | append: ';'
  assign list_style = list_style | append: ' --kf-testimonials-cols-tablet: ' | append: s.columns_tablet | append: ';'
  assign list_style = list_style | append: ' --kf-testimonials-cols-mobile: ' | append: s.columns_mobile | append: ';'
  assign list_style = list_style | append: ' --kf-testimonials-avatar: ' | append: s.avatar_size | append: 'px;'

  comment
    Stagger only means anything when the cards actually animate — the engine
    claims children carrying data-kf-animate, and there are none otherwise.
  endcomment
  assign use_stagger = false
  if s.animation != 'none'
    assign use_stagger = true
  endif
-%}

<div {% render 'kf-section-attrs', s: section.settings, extra_class: list_class %}>
  <div class="kf-container">
    {%- if has_header -%}
      <header class="kf-section-header kf-testimonials__header{% if s.alignment == 'center' %} kf-section-header--center{% endif %}">
        {%- if s.eyebrow != blank -%}
          <p class="kf-label kf-section-header__eyebrow">{{ s.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if s.heading != blank -%}
          <h2 class="{{ s.heading_size }}">{{ s.heading }}</h2>
        {%- endif -%}
        {%- if s.text != blank -%}
          <div class="kf-section-header__text kf-rte">{{ s.text }}</div>
        {%- endif -%}
      </header>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      {%- comment -%}
        Stagger contract: the LIST carries data-kf-stagger, each ITEM carries
        data-kf-animate. The grid's custom properties go through the same
        snippet — it emits its own `style` attribute, and a second one on the
        element would simply be ignored, taking the column counts with it.
      {%- endcomment -%}
      {%- liquid
        assign list_classes = 'kf-testimonials__list'
        assign item_classes = 'kf-testimonials__item'
        if is_carousel
          comment
            In carousel mode the track and slides are the slider's own classes,
            so the scroll-snap behaviour comes from component-slider.css rather
            than being restated here.
          endcomment
          assign list_classes = 'kf-slider__track kf-testimonials__track'
          assign item_classes = 'kf-slider__slide kf-testimonials__item'
        endif
        assign track_label = s.heading | default: 'Testimonials'
      -%}

      {%- if needs_slider -%}<kf-slider class="kf-testimonials__slider">{%- endif -%}

      {%- comment -%}
        The arrows MUST be inside <kf-slider>, and it is not a styling choice.
        The element finds them with `this.querySelector`, so arrows outside it
        are simply never wired up — and component-slider.css hides the track's
        scrollbar with `kf-slider:has([data-kf-slider-next])`, so that fails
        too and the bar stays on screen. One misplacement, two symptoms.

        Safe to render here unconditionally: `show_arrows` is one of the two
        things that make `needs_slider` true, so the wrapper always exists.
      {%- endcomment -%}
      {%- if is_carousel and s.show_arrows -%}
        {%- render 'kf-slider-controls', class: 'kf-testimonials__arrows' -%}
      {%- endif -%}

      <ul
        class="{{ list_classes }}"
        role="list"
        {% if is_carousel %}data-kf-slider-track tabindex="0" aria-label="{{ track_label | escape }}"{% endif %}
        {% render 'kf-motion-attrs',
          stagger: use_stagger,
          stagger_delay: s.animation_stagger_delay,
          style: list_style %}
      >
        {%- for block in section.blocks -%}
          {%- liquid
            assign b = block.settings
            assign quote_label = b.author | default: 'Customer'
          -%}
          <li
            class="{{ item_classes }}"
            {% if is_carousel %}data-kf-slider-slide{% endif %}
            {% render 'kf-motion-attrs', animation: s.animation %}
            {{ block.shopify_attributes }}
          >
            <figure class="kf-testimonials__card">
              {%- if b.rating > 0 -%}
                {%- comment -%}
                  Not a product review, so the announced sentence is written
                  here rather than borrowing the product one.
                {%- endcomment -%}
                {%- assign rating_label = b.rating | append: ' out of 5 stars' -%}
                <div class="kf-testimonials__rating">
                  {%- render 'kf-stars', rating: b.rating, scale_max: 5, label: rating_label -%}
                </div>
              {%- endif -%}

              {%- if b.quote != blank -%}
                <blockquote class="kf-rte kf-testimonials__quote">{{ b.quote }}</blockquote>
              {%- endif -%}

              {%- if b.author != blank or b.image != blank -%}
                <figcaption class="kf-testimonials__author">
                  {%- if b.image != blank -%}
                    <div class="kf-testimonials__avatar">
                      {%- render 'kf-media',
                        image: b.image,
                        ratio: 'square',
                        sizes: '80px',
                        alt: quote_label
                      -%}
                    </div>
                  {%- endif -%}

                  <div>
                    {%- if b.author != blank -%}
                      <p class="kf-testimonials__name kf-text-sm">{{ b.author | escape }}</p>
                    {%- endif -%}
                    {%- if b.meta != blank -%}
                      <p class="kf-testimonials__meta kf-text-xs">{{ b.meta | escape }}</p>
                    {%- endif -%}
                  </div>
                </figcaption>
              {%- endif -%}
            </figure>
          </li>
        {%- endfor -%}
      </ul>

      {%- if is_carousel -%}
        {%- render 'kf-slider-pagination', style: s.pagination, count: section.blocks.size -%}
      {%- endif -%}

      {%- if needs_slider -%}</kf-slider>{%- endif -%}
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.kf-testimonials.name",
  "tag": "section",
  "class": "kf-testimonials-section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.kf-testimonials.settings.header__1.content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:sections.kf-testimonials.settings.eyebrow.label"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:sections.kf-testimonials.settings.heading.label",
      "default": "What people say"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:sections.kf-testimonials.settings.heading_size.label",
      "options": [
        {
          "value": "kf-h3",
          "label": "t:sections.kf-testimonials.settings.heading_size.options__1.label"
        },
        {
          "value": "kf-h2",
          "label": "t:sections.kf-testimonials.settings.heading_size.options__2.label"
        },
        {
          "value": "kf-h1",
          "label": "t:sections.kf-testimonials.settings.heading_size.options__3.label"
        }
      ],
      "default": "kf-h2"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "t:sections.kf-testimonials.settings.text.label"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "t:sections.kf-testimonials.settings.alignment.label",
      "options": [
        {
          "value": "start",
          "label": "t:sections.kf-testimonials.settings.alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.kf-testimonials.settings.alignment.options__2.label"
        }
      ],
      "default": "start"
    },
    {
      "type": "header",
      "content": "t:sections.kf-testimonials.settings.header__2.content"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:sections.kf-testimonials.settings.layout.label",
      "options": [
        {
          "value": "grid",
          "label": "t:sections.kf-testimonials.settings.layout.options__1.label"
        },
        {
          "value": "carousel",
          "label": "t:sections.kf-testimonials.settings.layout.options__2.label"
        }
      ],
      "default": "grid",
      "info": "t:sections.kf-testimonials.settings.layout.info"
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "t:sections.kf-testimonials.settings.show_arrows.label",
      "default": true,
      "info": "t:sections.kf-testimonials.settings.show_arrows.info"
    },
    {
      "type": "select",
      "id": "pagination",
      "label": "t:sections.kf-testimonials.settings.pagination.label",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-testimonials.settings.pagination.options__1.label"
        },
        {
          "value": "dots",
          "label": "t:sections.kf-testimonials.settings.pagination.options__2.label"
        },
        {
          "value": "progress",
          "label": "t:sections.kf-testimonials.settings.pagination.options__3.label"
        }
      ],
      "default": "dots",
      "info": "t:sections.kf-testimonials.settings.pagination.info"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "label": "t:sections.kf-testimonials.settings.columns_desktop.label",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "range",
      "id": "columns_tablet",
      "label": "t:sections.kf-testimonials.settings.columns_tablet.label",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 2
    },
    {
      "type": "select",
      "id": "mobile_layout",
      "label": "t:sections.kf-testimonials.settings.mobile_layout.label",
      "options": [
        {
          "value": "grid",
          "label": "t:sections.kf-testimonials.settings.mobile_layout.options__1.label"
        },
        {
          "value": "scroll",
          "label": "t:sections.kf-testimonials.settings.mobile_layout.options__2.label"
        }
      ],
      "default": "scroll",
      "info": "t:sections.kf-testimonials.settings.mobile_layout.info"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "label": "t:sections.kf-testimonials.settings.columns_mobile.label",
      "options": [
        {
          "value": "1",
          "label": "t:sections.kf-testimonials.settings.columns_mobile.options__1.label"
        },
        {
          "value": "2",
          "label": "t:sections.kf-testimonials.settings.columns_mobile.options__2.label"
        }
      ],
      "default": "1",
      "info": "t:sections.kf-testimonials.settings.columns_mobile.info"
    },
    {
      "type": "range",
      "id": "avatar_size",
      "label": "t:sections.kf-testimonials.settings.avatar_size.label",
      "min": 32,
      "max": 72,
      "step": 4,
      "unit": "px",
      "default": 44
    },
    {
      "type": "header",
      "content": "t:sections.kf-testimonials.settings.header__3.content"
    },
    {
      "type": "select",
      "id": "animation",
      "label": "t:sections.kf-testimonials.settings.animation.label",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-testimonials.settings.animation.options__1.label"
        },
        {
          "value": "fade",
          "label": "t:sections.kf-testimonials.settings.animation.options__2.label"
        },
        {
          "value": "fade-up",
          "label": "t:sections.kf-testimonials.settings.animation.options__3.label"
        },
        {
          "value": "scale-up",
          "label": "t:sections.kf-testimonials.settings.animation.options__4.label"
        }
      ],
      "default": "fade-up"
    },
    {
      "type": "range",
      "id": "animation_stagger_delay",
      "label": "t:sections.kf-testimonials.settings.animation_stagger_delay.label",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "ms",
      "default": 70
    },
    {
      "type": "header",
      "content": "t:sections.kf-testimonials.settings.header__4.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.kf-testimonials.settings.color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "section_transition",
      "label": "t:sections.kf-testimonials.settings.section_transition.label",
      "info": "t:sections.kf-testimonials.settings.section_transition.info",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-testimonials.settings.section_transition.options__1.label"
        },
        {
          "value": "round",
          "label": "t:sections.kf-testimonials.settings.section_transition.options__2.label"
        },
        {
          "value": "overlap",
          "label": "t:sections.kf-testimonials.settings.section_transition.options__3.label"
        }
      ],
      "default": "none"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:sections.kf-testimonials.settings.padding_top.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:sections.kf-testimonials.settings.padding_bottom.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "t:sections.kf-testimonials.blocks.testimonial.name",
      "limit": 12,
      "settings": [
        {
          "type": "richtext",
          "id": "quote",
          "label": "t:sections.kf-testimonials.blocks.testimonial.settings.quote.label",
          "default": "<p>Genuinely the best thing I have bought this year. It arrived faster than I expected and it has not left my side since.</p>"
        },
        {
          "type": "text",
          "id": "author",
          "label": "t:sections.kf-testimonials.blocks.testimonial.settings.author.label",
          "default": "Sam R."
        },
        {
          "type": "text",
          "id": "meta",
          "label": "t:sections.kf-testimonials.blocks.testimonial.settings.meta.label",
          "info": "t:sections.kf-testimonials.blocks.testimonial.settings.meta.info"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.kf-testimonials.blocks.testimonial.settings.image.label"
        },
        {
          "type": "range",
          "id": "rating",
          "label": "t:sections.kf-testimonials.blocks.testimonial.settings.rating.label",
          "min": 0,
          "max": 5,
          "step": 1,
          "default": 5,
          "info": "t:sections.kf-testimonials.blocks.testimonial.settings.rating.info"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.kf-testimonials.presets.name",
      "blocks": [
        {
          "type": "testimonial"
        },
        {
          "type": "testimonial",
          "settings": {
            "author": "Alex M."
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "author": "Jordan P."
          }
        }
      ]
    }
  ]
}
{% endschema %}
