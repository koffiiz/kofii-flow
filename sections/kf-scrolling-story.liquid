{%- comment -%}
  Kofii Flow — Scrolling story
  ---------------------------------------------------------------------------
  Editorial storytelling: a media column pinned with `position: sticky` while
  the text steps scroll past it, the image cross-fading to match whichever step
  is being read.

  Blocks are section-local, not theme blocks, and that is the deciding
  constraint: the steps have to be iterated TWICE — once to build the pinned
  media stack, once for the text column — and `{% content_for 'blocks' %}`
  renders blocks without letting the section read their settings.

  Each step's media is therefore output in two places. Only one is ever
  displayed (the other is `display: none`, which browsers do not fetch), so
  this costs markup, not requests. The inline copy is what makes the section
  degrade honestly.

  Degradation, in order of severity:
    no JavaScript   the pinned layout is never applied; steps render as a
                    plain stacked sequence, each with its own image
    small screens   same stacked sequence — sticky side-by-side has nowhere
                    to go in a narrow column
    reduced motion  layout is unchanged; the cross-fade becomes instant via
                    the global reduced-motion rule

  No scroll-jacking anywhere. See assets/kf-scroll-story.js.
{%- endcomment -%}

{{ 'section-scrolling-story.css' | asset_url | stylesheet_tag }}
<script src="{{ 'kf-scroll-story.js' | asset_url }}" defer></script>

{%- liquid
  assign s = section.settings

  assign story_class = 'kf-story kf-story--' | append: s.layout
  if s.dim_inactive
    assign story_class = story_class | append: ' kf-story--dim'
  endif

  case s.media_width
    when 'small'
      assign media_track = '0.8fr'
    when 'large'
      assign media_track = '1.25fr'
    else
      assign media_track = '1fr'
  endcase

  assign root_style = '--kf-story-media: ' | append: media_track | append: ';'
  assign media_sizes = '(min-width: 990px) 50vw, 100vw'
-%}

<div
  {% render 'kf-section-attrs',
    s: section.settings,
    extra_class: story_class,
    style: root_style %}
>
  <div class="kf-container">
    {%- if s.heading != blank or s.eyebrow != blank -%}
      <header class="kf-section-header kf-story__header">
        {%- if s.eyebrow != blank -%}
          <p class="kf-label kf-section-header__eyebrow">{{ s.eyebrow | escape }}</p>
        {%- endif -%}
        {%- if s.heading != blank -%}
          <h2 class="{{ s.heading_size }}">{{ s.heading }}</h2>
        {%- endif -%}
      </header>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <kf-scroll-story class="kf-story__inner">
        {%- comment -%}
          The pinned media stack. Every frame occupies the same grid cell and
          only the active one is opaque. Hidden from assistive technology: it
          is a visual echo of the media already inside each step.
        {%- endcomment -%}
        <div class="kf-story__media" aria-hidden="true">
          {%- for block in section.blocks -%}
            <div
              class="kf-story__frame"
              data-kf-story-frame
              {% if forloop.first %}data-active{% endif %}
            >
              {%- render 'kf-media',
                image: block.settings.image,
                ratio: s.media_ratio,
                sizes: media_sizes,
                placeholder: 'lifestyle-1'
              -%}
            </div>
          {%- endfor -%}
        </div>

        <ol class="kf-story__steps" role="list">
          {%- for block in section.blocks -%}
            <li
              class="kf-story__step"
              data-kf-story-step
              {% if forloop.first %}data-active{% endif %}
              {{ block.shopify_attributes }}
            >
              {%- comment -%}
                Shown only when the pinned layout is not in play — small
                screens and no JavaScript.
              {%- endcomment -%}
              <div class="kf-story__step-media">
                {%- render 'kf-media',
                  image: block.settings.image,
                  ratio: s.media_ratio,
                  sizes: '100vw',
                  placeholder: 'lifestyle-1'
                -%}
              </div>

              <div class="kf-story__step-body">
                {%- if s.show_numbers -%}
                  {%- assign step_number = forloop.index | prepend: '0' | slice: -2, 2 -%}
                  <span class="kf-story__number kf-label" aria-hidden="true">{{ step_number }}</span>
                {%- endif -%}

                {%- if block.settings.eyebrow != blank -%}
                  <p class="kf-label kf-story__eyebrow">{{ block.settings.eyebrow | escape }}</p>
                {%- endif -%}

                {%- if block.settings.heading != blank -%}
                  <h3 class="kf-story__heading {{ s.step_heading_size }}">{{ block.settings.heading }}</h3>
                {%- endif -%}

                {%- if block.settings.text != blank -%}
                  <div class="kf-rte kf-story__text">{{ block.settings.text }}</div>
                {%- endif -%}

                {%- if block.settings.link_label != blank -%}
                  {%- render 'kf-button',
                    label: block.settings.link_label,
                    href: block.settings.link,
                    style: 'plain',
                    icon: 'arrow'
                  -%}
                {%- endif -%}
              </div>
            </li>
          {%- endfor -%}
        </ol>
      </kf-scroll-story>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.kf-scrolling-story.name",
  "tag": "section",
  "class": "kf-story-section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.kf-scrolling-story.settings.header__1.content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:sections.kf-scrolling-story.settings.eyebrow.label"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:sections.kf-scrolling-story.settings.heading.label",
      "default": "How it is made"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:sections.kf-scrolling-story.settings.heading_size.label",
      "options": [
        {
          "value": "kf-h3",
          "label": "t:sections.kf-scrolling-story.settings.heading_size.options__1.label"
        },
        {
          "value": "kf-h2",
          "label": "t:sections.kf-scrolling-story.settings.heading_size.options__2.label"
        },
        {
          "value": "kf-h1",
          "label": "t:sections.kf-scrolling-story.settings.heading_size.options__3.label"
        }
      ],
      "default": "kf-h2"
    },
    {
      "type": "select",
      "id": "step_heading_size",
      "label": "t:sections.kf-scrolling-story.settings.step_heading_size.label",
      "options": [
        {
          "value": "kf-h4",
          "label": "t:sections.kf-scrolling-story.settings.step_heading_size.options__1.label"
        },
        {
          "value": "kf-h3",
          "label": "t:sections.kf-scrolling-story.settings.step_heading_size.options__2.label"
        },
        {
          "value": "kf-h2",
          "label": "t:sections.kf-scrolling-story.settings.step_heading_size.options__3.label"
        }
      ],
      "default": "kf-h3"
    },
    {
      "type": "header",
      "content": "t:sections.kf-scrolling-story.settings.header__2.content"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:sections.kf-scrolling-story.settings.layout.label",
      "options": [
        {
          "value": "media-left",
          "label": "t:sections.kf-scrolling-story.settings.layout.options__1.label"
        },
        {
          "value": "media-right",
          "label": "t:sections.kf-scrolling-story.settings.layout.options__2.label"
        }
      ],
      "default": "media-left"
    },
    {
      "type": "select",
      "id": "media_width",
      "label": "t:sections.kf-scrolling-story.settings.media_width.label",
      "options": [
        {
          "value": "small",
          "label": "t:sections.kf-scrolling-story.settings.media_width.options__1.label"
        },
        {
          "value": "medium",
          "label": "t:sections.kf-scrolling-story.settings.media_width.options__2.label"
        },
        {
          "value": "large",
          "label": "t:sections.kf-scrolling-story.settings.media_width.options__3.label"
        }
      ],
      "default": "medium"
    },
    {
      "type": "select",
      "id": "media_ratio",
      "label": "t:sections.kf-scrolling-story.settings.media_ratio.label",
      "options": [
        {
          "value": "square",
          "label": "t:sections.kf-scrolling-story.settings.media_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.kf-scrolling-story.settings.media_ratio.options__2.label"
        },
        {
          "value": "tall",
          "label": "t:sections.kf-scrolling-story.settings.media_ratio.options__3.label"
        },
        {
          "value": "landscape",
          "label": "t:sections.kf-scrolling-story.settings.media_ratio.options__4.label"
        }
      ],
      "default": "portrait"
    },
    {
      "type": "checkbox",
      "id": "show_numbers",
      "label": "t:sections.kf-scrolling-story.settings.show_numbers.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "dim_inactive",
      "label": "t:sections.kf-scrolling-story.settings.dim_inactive.label",
      "default": true,
      "info": "t:sections.kf-scrolling-story.settings.dim_inactive.info"
    },
    {
      "type": "header",
      "content": "t:sections.kf-scrolling-story.settings.header__3.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.kf-scrolling-story.settings.color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "section_transition",
      "label": "t:sections.kf-scrolling-story.settings.section_transition.label",
      "info": "t:sections.kf-scrolling-story.settings.section_transition.info",
      "options": [
        {
          "value": "none",
          "label": "t:sections.kf-scrolling-story.settings.section_transition.options__1.label"
        },
        {
          "value": "round",
          "label": "t:sections.kf-scrolling-story.settings.section_transition.options__2.label"
        },
        {
          "value": "overlap",
          "label": "t:sections.kf-scrolling-story.settings.section_transition.options__3.label"
        }
      ],
      "default": "none"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:sections.kf-scrolling-story.settings.padding_top.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:sections.kf-scrolling-story.settings.padding_bottom.label",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "t:sections.kf-scrolling-story.blocks.step.name",
      "limit": 8,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.image.label"
        },
        {
          "type": "text",
          "id": "eyebrow",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.eyebrow.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.heading.label",
          "default": "The first step"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.text.label",
          "default": "<p>One paragraph on this stage of the story. Keep it to something a reader can take in while the image is on screen.</p>"
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.link_label.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.kf-scrolling-story.blocks.step.settings.link.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.kf-scrolling-story.presets.name",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "heading": "It starts with the fibre"
          }
        },
        {
          "type": "step",
          "settings": {
            "heading": "Then the loom"
          }
        },
        {
          "type": "step",
          "settings": {
            "heading": "And finally, the finish"
          }
        }
      ]
    }
  ]
}
{% endschema %}
